@rules/core-identity.md
@rules/core-conventions.md

## Commands

Slash commands are in `commands/`. Each `.md` file is a command (e.g. `/frontend-design`). Read the file when the user invokes the command. Commands are gitignored and installed locally via `scripts/anthropic-skill` or `scripts/matt-skill`.
